#ifndef TENSORFLOW_LITE_MODEL_DATA_H_
#define TENSORFLOW_LITE_MODEL_DATA_H_

// =============================================================================
// ==                             重要说明                                   ==
// =============================================================================
//
// 这是一个占位符模型文件。您需要将自己训练好的 TensorFlow Lite 模型
// (一个 `.tflite` 文件) 转换为 C 语言数组，然后替换掉下面的内容。
//
// 如何转换?
// 1. 您需要一个名为 `xxd` 的命令行工具 (Linux/macOS 自带, Windows需另外安装)。
// 2. 运行以下命令:
//    xxd -i your_model.tflite > model_data.cc
// 3. 上述命令会生成一个 `model_data.cc` 文件，其中包含一个类似下面的数组。
// 4. 将该文件中的数组定义拷贝到这里，替换掉 `g_model_data` 的定义，
//    并确保数组名称和长度变量名称与代码中的使用保持一致。
//
// =============================================================================


// 这是一个空的、无效的模型数据，仅用于让项目能够成功编译。
// 您必须用您自己的模型数据来替换它。
unsigned char g_model_data[] = { 0x00 };
unsigned int g_model_data_len = 1;

#endif // TENSORFLOW_LITE_MODEL_DATA_H_